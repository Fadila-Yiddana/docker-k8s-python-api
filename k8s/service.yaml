apiVersion: v1
kind: Service
metadata:
  name: docker-k8s-python-api-service
spec:
  type: NodePort
  selector:
    app: docker-k8s-python-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
